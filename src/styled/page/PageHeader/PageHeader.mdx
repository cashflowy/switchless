import {Canvas, Meta, Subtitle, Primary, Controls, Stories} from '@storybook/blocks';
import { Alert } from '@mui/joy';
import InfoIcon from '@mui/icons-material/Info';
import CheckCircleIcon from '@mui/icons-material/CheckCircle';


import * as PageHeaderStories from './PageHeader.stories';
import PageHeader from './PageHeader';

<Meta of={PageHeaderStories} />

# PageHeader Component

<Subtitle>
  An enhanced and flexible page header component with breadcrumbs and action buttons.
</Subtitle>
<Alert 
  startDecorator={<CheckCircleIcon />} 
  sx={{ mb: 2 }} 
  color="success" 
  variant="soft"
>
  This component is fully implemented and ready for use in your projects.
</Alert>

<Alert startDecorator={<InfoIcon />} sx={{ mb: 2 }} color="info" variant="soft">
  The PageHeader component is designed to be responsive, adjusting its layout for different screen sizes. On smaller screens, it may stack elements vertically for better readability.
</Alert>

## Overview

The `PageHeader` component is designed to provide a consistent and customizable header for your pages. It includes support for breadcrumbs, a main title, and optional action buttons.

## Basic Usage




```jsx
import { PageHeader } from 'switchless';

<PageHeader header="Dashboard"/>
```

## Configurable Example

<Primary />

<Controls />
## Props

The `PageHeader` component accepts the following props:

- `header`: (required) The main title of the page. Can be:
  - A string for a simple header
  - An object with `part1` and `part2` properties for a two-part header with different styling
- `headerLevel`: (optional) The heading level for the main title. Defaults to 'h3'.
- `breadcrumbs`: (optional) An array of breadcrumb objects with `text` and optional `href` properties.
- `RightButtons`: (optional) A component or function that returns action buttons for the header.

## Examples

### Basic Header

<Canvas of={PageHeaderStories.Default} />

PageHeader rendered as a simple header with a title.

### Varying Header Sizes


<Canvas of={PageHeaderStories.VaryingHeaderSizes} />

The default header size is `h3`. You can pass a different header level to the `headerLevel` prop to change the size.

### With Breadcrumbs

<Canvas of={PageHeaderStories.WithBreadcrumbs} />

PageHeader rendered with breadcrumbs.

### With Right Buttons

<Canvas of={PageHeaderStories.WithRightButtons} />

PageHeader rendered with right-aligned buttons.


## Header as an Object
You can pass an object to the `header` prop to create a two-part or three-part header.
The object can have `part1`, `part2`, and `part3` properties. Each with a different styling hardcoded in the component.

```jsx
<PageHeader 
header={{ part1: 'Three ', part2: 'Part', part3: 'Header' }} 
/>

```

### Two-Part Header

<Canvas of={PageHeaderStories.TwoPartHeader} />



### Three-Part Header

<Canvas of={PageHeaderStories.ThreePartHeader} />





