import {Canvas, Meta, Subtitle, Primary, Controls, Stories} from '@storybook/blocks';


import * as TabNavigationStories from './TabNavigation.stories';
import TabNavigation from './TabNavigation';


import { Alert } from '@mui/joy';
import InfoIcon from '@mui/icons-material/Info';
import CheckCircleIcon from '@mui/icons-material/CheckCircle';


<Meta of={TabNavigationStories} />

# TabNavigation Component

<Subtitle>
  A flexible tab navigation component for easy navigation between different sections.
</Subtitle>

<Alert startDecorator={<CheckCircleIcon />} color="success" variant="soft" 
sx={{ mb: 2 }} >
  This component is fully implemented and ready for use in your projects.
</Alert>


<Alert startDecorator={<InfoIcon />} sx={{ mb: 2 }} color="info" variant="soft">
  The TabNavigation component is designed to work seamlessly with Next.js 14 app router. It automatically handles URL updates for tab navigation.
</Alert>

## Overview

The `TabNavigation` component provides a customizable tab interface for navigating between different sections of your application. It supports both horizontal and vertical layouts and can display optional count badges for each tab.

## Basic Usage

```jsx
import { TabNavigation } from 'switchless';
const tabs = [
{ header: 'Tab 1', count: 5 },
{ header: 'Tab 2', count: 0 },
{ header: 'Tab 3', count: 10 },
{ header: 'Tab 4' },
];
<TabNavigation tabHeaders={tabs} />
```

## Usage Notes

1. The component automatically updates the URL based on the selected tab, making it easy to share links to specific tabs.
2. Tab headers are converted to URL-friendly formats (lowercase, spaces replaced with hyphens).
3. The component is responsive and supports horizontal scrolling on smaller screens.
4. The componnent updates the URL. To avoid navigation issues Create desired routes or redirect to the desired routes.

## Props

The TabNavigation component accepts the following props:

- `tabHeaders`: (required) An array of tab objects with `header` and optional `count` properties.
- `vertical`: (optional) Boolean to set the orientation of tabs. Defaults to false (horizontal).

## Configurable Example

<Primary />

<Controls />

## Examples

### Default Horizontal Tabs

<Canvas of={TabNavigationStories.Default} />

TabNavigation rendered with horizontal tabs and count Chips.

### Vertical Tabs

<Canvas of={TabNavigationStories.VerticalTabs} />

TabNavigation rendered with vertical orientation.

### Tabs Without Counts

<Canvas of={TabNavigationStories.WithoutCounts} />

TabNavigation rendered without count badges.


