import {Canvas, Meta, Subtitle, Primary, Controls, Stories} from '@storybook/blocks';
import { Alert } from '@mui/joy';
import CheckCircleIcon from '@mui/icons-material/CheckCircle';

import * as LogoStories from './Logo.stories';
import Logo from './Logo';



<Meta of={LogoStories} />

# Logo Component

<Subtitle>
 Logo component for Cashflowy Applications.
</Subtitle>
<Alert 
  startDecorator={<CheckCircleIcon />} 
  sx={{ mb: 2 }} 
  color="success" 
  variant="soft"
>
  This component is fully implemented and ready for use in your projects.
</Alert>


The Logo component displays a logo with optional customization options.


```jsx
import { Logo } from 'switchless';

```
## Props

The Logo component accepts the following props:

- `offering`: (optional) Custom text to display alongside the logo
- `href`: (optional) Custom link destination

## Configurable Example

<Primary />

<Controls />

## Examples

### Custom Offering
You can customize the offering text:

<Canvas of={LogoStories.Fetch}/>
 

 