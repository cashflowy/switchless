import { Chip } from '@mui/joy'
import { Meta, Story, Canvas, Subtitle, ArgsTable } from '@storybook/blocks';
import * as PricingSectionStories from './PricingSection.stories';

<Meta of={PricingSectionStories} />
# PricingSection <Chip size="sm" variant="outlined" color="primary" sx={{ ml: 1, verticalAlign: 'middle'}}>Maturity = complete </Chip>


Component for displaying pricing section in Landing Page

## Code

```jsx
'use client'
import React from 'react'
import { Box, Card, Chip, Typography, Button, List, ListItem, ListItemDecorator, Divider, CardActions } from '@mui/joy';
import Check from '@mui/icons-material/Check';
import Close from '@mui/icons-material/Close';
import KeyboardArrowRight from '@mui/icons-material/KeyboardArrowRight';

export default function PricingSection({ priceList }) {
    return (
        <>
            <Box
                sx={{
                    width: '100%',
                    display: 'flex',
                    justifyContent: 'center',
                    gap: 2,
                    mx:2
                }}
            >
                {priceList && priceList.map((li) => {
                    return (
                        <Card size="lg" variant="outlined">
                            <Chip size="sm" variant="outlined" color={li.type.color}>
                                {li.type.text}
                            </Chip>
                            <Typography level="h2">{li.header}</Typography>
                            <Divider inset="none" />
                            <List size="sm" sx={{ mx: 'calc(-1 * var(--ListItem-paddingX))' }}>
                                {li.featuresIncluded?.map((feature) => {
                                    return (
                                        <ListItem>
                                            <ListItemDecorator sx={{color:'success.500'}}>
                                                <Check fontSize='small'/>
                                            </ListItemDecorator>
                                            {feature}
                                        </ListItem>
                                    )
                                })}

                                {li.featuresExcluded?.map((feature) => {
                                    return (
                                        <ListItem>
                                            <ListItemDecorator sx={{color:'danger.500'}}>
                                                <Close fontSize='small'/>
                                            </ListItemDecorator>
                                            {feature}
                                        </ListItem>
                                    )
                                })}
                            </List>
                            <Divider inset="none" />
                            <CardActions>
                                {li.monthlyPrice && <Typography level="title-lg" sx={{ mr: 'auto' }}>
                                    ₹{li.monthlyPrice}{' '}
                                    <Typography textColor="text.tertiary" sx={{ fontSize: 'sm' }}>
                                        / month
                                        </Typography>
                                    </Typography>
                                }
                                <Button
                                    variant="soft"
                                    color="neutral"
                                    endDecorator={<KeyboardArrowRight />}
                                    component='a'
                                    target='_blank'
                                    href={li.signUpButton.link}
                                >
                                    {li.signUpButton.text}
                                </Button>
                            </CardActions>
                        </Card>
                    )

                })}

            </Box>
        </>
    )
}
```

## Example Usage

```jsx
import PricingSection from '@/components/PricingSection'
import { Typography, Box } from '@mui/joy'

function MyComponent() {    

    priceList: [
      {
        type: {text:'Basic',color:'neutral'},
        header: 'Hobbyists',
        monthlyPrice: 50,
        featuresIncluded: [
          'One click data update',
          'No more manually creating reports every month',
          'Work on improving the reports',
        ],
        featuresExcluded: [
          'No custom models',
          'No custom models',
          'No custom models',
        ],
        signUpButton: { text: 'Sign up', link: 'https://fetch.cashflowy.io/signup' }
      },
      // ... remaining price list items ...
    ]

  return (
    <Typography level='h1'  sx={{textAlign:'center',mt:4}}>Pricing</Typography>
      <Box id='pricing' sx={{ display: 'flex', justifyContent: 'center', mt: 0 }}>
        <PricingSection priceList={priceList} />
      </Box>
  )
}
```

## Demo

<div style={{
  position: 'relative',
  paddingBottom: '56.25%',
  height: 0
}}>
  <iframe 
    src="https://www.loom.com/embed/c5a84e6c442f4aba98df1b27960f2fbc?sid=c2126045-e56e-429b-ab27-b7e14d10a467" 
    frameborder="0" 
    webkitallowfullscreen 
    mozallowfullscreen 
    allowfullscreen 
    style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%'
    }}
  />
</div> 
